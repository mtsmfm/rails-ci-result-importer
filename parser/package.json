{
  "name": "app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "pegjs": "^0.10.0"
  },
  "scripts": {
    "build": "yarn build:travis_result && yarn build:rails_ci",
    "build:travis_result": "yarn pegjs --format globals --export-var TravisResultParser --output out/travis_result.js travis_result.pegjs",
    "build:rails_ci": "yarn pegjs --format globals --export-var RailsCiParser --output out/rails_ci.js rails_ci.pegjs",
    "test": "ts-node test.ts"
  },
  "devDependencies": {
    "@types/node": "^11.11.0",
    "@types/pegjs": "^0.10.1",
    "@types/request-promise": "^4.1.42",
    "request": "^2.88.0",
    "request-promise": "^4.2.4",
    "ts-node": "^8.0.3",
    "typescript": "^3.3.3333"
  }
}
